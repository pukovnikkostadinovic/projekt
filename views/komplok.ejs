<% include partials/header.ejs %>
    <div class="table-wrapper">
        <% if (komplok.length > 0) {%>
            <table class="table table-hovered">
	<h2><strong><%= komplok[0].ime_komponente %></strong></h2>
                <thead class="thead-dark">
                    <tr>
                        <th scope="col">Količina</th>
			<th scope="col">Lokacija</th>
			<th scope="col"></th>
                    </tr>
                </thead>
                <tbody>
                    <% komplok.forEach((komp, index) => { %>
                        <tr>
                            <td><input type"number" name="ante" value="<%= komp.kolicina %>"></td>
			    <td><%= komp.ime_lokacije %></td>
			   <td>
                                <a href="/izmjeni_komplok/<%= komp.id %>" target="_blank" rel="noopener" class="btn btn-sm btn-success">Izmjeni</a>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
            
	<a href="/dod_komp_lok/<%= kid %>" target="_blank" rel="noopener" class="btn btn-sm btn-success">Dodaj</a>
        <% } else { %>
	<a href="/dod_komp_lok/<%= kid %>" target="_blank" rel="noopener" class="btn btn-sm btn-success">Dodaj</a>
        <% } %>
    </div>
</div>
</body>
</html>